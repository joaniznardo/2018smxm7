# lab20
Exemple de Split Horizon amb bind9 i docker

Presentem dues visions del mateix domini: la interna (LAN) i l'externa (WAN)

recomanació: comprovar la resolució dels recursos des d'el client intern i extern.

